<%= form_for @post, :html => {:multipart => true} do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :tag_list, 'Tags' %>
	<%= f.text_field :tag_list %>

	<%= f.label :image %>
	<%= f.file_field :image %>
	
	<%= f.submit %>
<% end %>